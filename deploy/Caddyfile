# Caddy auto-provisions and renews a Let's Encrypt cert for $MCP_DOMAIN and
# reverse-proxies it to the MCP container. Values come from deploy/.env.
{
	email {$ACME_EMAIL}
}

{$MCP_DOMAIN} {
	reverse_proxy gtfs-one-mcp:3000
}
